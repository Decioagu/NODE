const nome = 'Décio Santana de Aguiar'

for (let index = 0; index < 7; index++) {
    console.log(nome);  
}

// estrutura "setTimeout", executa uma tarefa apos tempo detreminado em mili-segundo
setTimeout(() => {
    console.clear()
}, 3000);
